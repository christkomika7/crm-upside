{
  "name": "creo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "web:dev": "cd web && bun run dev",
    "wd:build": "cd web && bun run build",
    "server:dev": "cd server && bun run dev"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  }
}